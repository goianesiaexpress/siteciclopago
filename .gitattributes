# APK - tratar como binário (evita corromper em merge)
*.apk binary

# Se no futuro o APK passar de 100MB, descomente a linha abaixo e instale Git LFS:
# *.apk filter=lfs diff=lfs merge=lfs -text

# Normalização de quebra de linha
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.json text eol=lf
*.sql text eol=lf
